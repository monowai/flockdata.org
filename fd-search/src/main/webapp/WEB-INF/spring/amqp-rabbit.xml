<?xml version="1.0" encoding="UTF-8"?>

<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
                        http://www.springframework.org/schema/beans/spring-beans.xsd
		                http://www.springframework.org/schema/rabbit
		                http://www.springframework.org/schema/rabbit/spring-rabbit.xsd http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd">

    <rabbit:connection-factory id="connectionFactory"
                               host="${rabbit.host}"
                               port="${rabbit.port}"
                               channel-cache-size="${rabbit.publisherCacheSize}"
                               publisher-confirms="${rabbit.publisher.confirms}"
                               publisher-returns="${rabbit.publisher.returns}"
                               executor="rabbitTaskExec"/>

    <task:executor id="rabbitTaskExec"
                   pool-size="${rabbit.thread.poolSize}"
                   queue-capacity="${rabbit.task.capacity}"
                   rejection-policy="CALLER_RUNS"/>

    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory"/>

    <rabbit:admin connection-factory="connectionFactory"/>

    <rabbit:queue name="${fd.search.queue}"/>
    <rabbit:queue name="${fd.engine.queue}"/>

    <rabbit:direct-exchange name="${fd.search.exchange}">
        <rabbit:bindings>
            <rabbit:binding queue="${fd.search.queue}" key="${fd.search.binding}"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <rabbit:direct-exchange name="${fd.engine.exchange}">
        <rabbit:bindings>
            <rabbit:binding queue="${fd.engine.queue}" key="${fd.engine.binding}"/>
        </rabbit:bindings>
    </rabbit:direct-exchange>


</beans>
