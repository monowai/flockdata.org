The folder name must match the fully qualified index root.

This version supports tests running where fd.search.index.typeSuffix=true

if you change the search prefix to anything other than fd. (the default) then you would have to
also rename this folder